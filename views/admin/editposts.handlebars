{{#each errors}}  
  <div class="alert alert-danger">{{text}}</div>

{{else}}

{{/each}}

<div class="card">
  <div class="card-body">
    <h4>Edit Post</h4>
    <form action="/admin/post/edit" method="POST">
    <input type="hidden" name="id" value="{{post._id}}">
      <label for="title">Title:</label>
      <input type="text" name="title" id="ititle" class="form-control" value="{{post.title}}">
      <label for="slug">Slug:</label>
      <input type="text" name="slug" id="islug" class="form-control" value="{{post.slug}}">
      <label for="description">Description:</label>
      <input type="text" name="description" id="idescription" class="form-control" value="{{post.description}}">
      <label for="content">Content:</label>
      <textarea name="content" id="icontent" cols="30" rows="10" class="form-control">{{post.content}}</textarea>
      <label for="category">Category:</label>
      <select name="category" id="icategory" class="form-select">
        {{#each categories}}
          <option value="{{_id}}">{{name}}</option>
        {{else}}
          <option value="0">No category registered</option>
        {{/each}}
      </select>
      <button class="btn btn-success btn-sm mt-3" type="submit">Edit Post</button>
    </form>
  </div>
</div>